@model TrainHoppersTARpe23.Models.Powerups.PowerupImageViewModel

<div class="form-group row col-sm-4 offset-2">
    <div>@Model.ImageTitle</div>
    @if (!Model.Image.Any())
    {
        <p>"No images are found"</p>
    }
    <img class="card-image-top" src="@Model.Image" asp-append-version="true"/>
</div>